# Draft a communication

Gmail MCP was `needsAuth` at setup. Zoom MCP was `needsAuth`. Continue with drafts.

1. Retrieve relevant evidence and keep attribution.
2. Match approved writing examples when present.
3. Verify recipients and the sending identity. User-reported Gmail is `gmail-fashiobusiness`. Firebase/git email observed is `zakiymanigo@gmail.com`. Do not silently pick one.
4. Produce a draft. Sending is a separate, explicit step.
5. For Zoom: prepare agendas or notes. Do not create or alter meetings until Zoom is authenticated and the user asks.

Use `ops/templates/communication-draft.md`.
