# Draft a communication

Gmail MCP is signed in as a FITD Sent mailbox (`fitdadmin@fitdai.com`). This repo requires `zakiymanigo@gmail.com`. Zoom catalog is `needsAuth`; Zoom bridge failed DCR. Continue with drafts.

1. Retrieve relevant evidence and keep attribution.
2. Match approved writing examples when present.
3. Verify recipients and the sending identity. For this repo that is `zakiymanigo@gmail.com`. If MCP is on another mailbox, stop — do not send, do not switch silently.
4. Produce a draft. Sending is a separate, explicit step.
5. For Zoom: prepare agendas or notes. Do not create or alter meetings until Zoom actually works and the user asks.

Use `ops/templates/communication-draft.md`.
