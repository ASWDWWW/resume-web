# Career-site media URLs

Uploaded 17 September 2026 to bucket `zakiymanigo-career.firebasestorage.app` as `zakiymanigo@gmail.com`. Prefix: `career-media/`. All 20 objects returned HTTP 200 on a sample HEAD.

Public download tokens live in `ops/product-engineering/media-urls.json` and `public/media.js`. Local copies (not a replacement for Storage) are under `public/media/` for fallback.

`storage.rules` is in the repo. **Rules were not deployed** (out of scope). Token URLs work without a rules deploy.

Do not upload transcripts or job-title packets here.
